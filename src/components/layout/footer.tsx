function Footer() {
  return (
    <div
      style={{padding: "8px",backgroundColor: "#000",color: "#fff",}}>
      <p>Cocktail - Application / {new Date().getFullYear()}</p>
    </div>
  );
}

export default Footer;
